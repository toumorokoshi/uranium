#!/usr/bin/env python
import os
import sys
from setuptools import setup, find_packages

setup(name='example',
      description='example',
      packages=find_packages(),
      install_requires=[],
      classifiers=[])
